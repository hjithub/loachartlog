name: Fetch Price Movers

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch top price movers from Lostark API
        env:
          LOSTARK_API_KEY: ${{ secrets.LOSTARK_API_KEY }}
        run: python3 scripts/fetch_movers.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/movers.json docs/prices.json
          git diff --staged --quiet && echo "No changes" && exit 0
          git commit -m "Update price movers $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
